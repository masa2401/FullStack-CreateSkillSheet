# 設計ノート

[スキルシート作成ページ（フルスタック版）](./README.md)の設計判断と、その背景をまとめた文書です。\
機能の使い方ではなく、なぜその実装を選んだかを扱います。

## 状態に応じた段階的な機能制限

### aria-disabled によるアクセシブルな操作誘導

名前が未入力のゲスト状態でも、結果画面の閲覧は許可します。\
一方で、共有・印刷のようにサーバー側のデータと紐づく操作は、名前が確定するまで実行できません。

この「見えるが押せない」状態を native の `disabled` で表現すると、hover も focus も発生しません。\
`disabled` の要素はマウスやキーボードのイベントを受け取らないため、\
押せない理由をツールチップで説明できなくなります。

採用したのは `aria-disabled` による無効化です。\
`AppButton.vue` が `inactive` プロパティを受け取り、属性とスタイルを制御します。

```vue
:aria-disabled="inactive"
```

```
aria-disabled:cursor-not-allowed aria-disabled:opacity-60 aria-disabled:shadow-none
```

要素自体は有効なままなので、hover と focus が通常どおり発生します。\
`ShareButton.vue` はゲスト状態のときだけボタンを `Tooltip` で包み、\
`useGuestGate.ts` の `GUEST_HINT_MESSAGE` を表示します。

操作の抑止は同じコンポーザブルの `guard()` が担当します。\
ゲスト状態なら本来の処理を実行せず、名前入力欄へフォーカスを移してページ先頭へスクロールします。

```ts
const guard = (action: () => void): void => {
  if (isGuest.value) {
    goToNameInput();
    return;
  }
  action();
};
```

`AppButton.vue` はクリックを止めていません。\
`type="button"` でフォーム送信だけを封じ、抑止は呼び出し側の `guard()` に委ねています。\
`aria-disabled` の要素はタブ順に残るため、キーボードでもボタンへ到達できます。

ツールチップで押せない理由を示し、フォーカス移動で次の操作へ誘導するため、\
ポインタ操作でもキーボード操作でも同じ流れになります。

## Lambda のコールドスタートを見越したプリフェッチ設計

### 名前確定をトリガーにした先行保存

PDF 化は AWS Lambda 上の Puppeteer で行います。\
コンテナイメージを使うためコールドスタートが発生し、完了まで実測で 7 秒前後かかります。\
共有メニューを開いてから保存を始めると、この待ち時間が体感の遅さになります。

Provisioned Concurrency による常時起動も検討しましたが、\
アクセス頻度が低いアプリケーションではコストに見合わないため見送りました。

代わりに保存処理を前倒しします。\
結果ページで名前の入力が確定した時点（`ResultPage.vue` の `handleNameCommitted`）で、\
シートの保存を先行実行します。

保存 API は `SkillSheetService.save()` の中で、保存成功後に Lambda を非同期 Invoke します。\
ユーザーが結果を読んでいる間に PDF の生成が進みます。

二重保存を避けるため、`useSurveyStore.ts` の `getSavedIdOrSave()` が入口を一本化します。

- 保存済みで、回答内容がスナップショットと一致する場合は既存の ID を返す
- ID の実在を未確認の場合のみ、バックエンドへ存在確認を投げる
- それ以外は新規に保存する
  共有メニューを開く `ShareButton.vue` の `openMenu()` も同じ関数を呼ぶため、\
  プリフェッチが失敗してもボタン操作から機能は完結します。

## テストと CI による品質向上

### アクセシビリティを軸にしたテスト設計

CSS クラス名に依存したテストは、見た目のリファクタのたびに壊れます。\
shadcn-vue への移行でもクラス構成が大きく変わりました。

検証軸は、表示テキスト・`role`・アクセシブル名・ネイティブ要素の状態・`data-*` 属性に統一しています。

状態を属性で表す場合は専用の属性を切ります。\
`MenuItemButton.vue` はフィードバック状態を `data-feedback` で持ち、\
Tailwind のデータ属性バリアントで見た目を切り替えます。\
shadcn 側の `data-variant` と衝突させないための命名です。

### Vitest と Playwright の使い分け

すべてを Vitest（jsdom）で書くと、\
CSS の `:has()` セレクタ、Clipboard API、`localStorage` の永続化、印刷用スタイル、\
Reka UI の Portal 経由の描画といった、実ブラウザでしか発生しない挙動を検証できません。\
Playwright に寄せると、多分岐のロジックや emit されたペイロードの値レベルの検証が難しく、\
実行コストも膨らみます。

Vitest でロジックを、Playwright で見た目と実挙動を検証する形で分けています。\
Reka UI の開閉、Portal の描画位置、印刷レイアウトの退行は e2e で確認します。

### 待ち時間と状態を外から注入する E2E

CSV ダウンロードや印刷スタイルのように特定の画面状態だけを確かめるテストで、\
トップ画面からクリック操作で状態を作ると、実行時間と可読性を損ないます。

`pinia-plugin-persistedstate` が書き出す構造を調査し、\
`addInitScript` で `localStorage` に直接状態を書き込んでから、対象ページへ遷移します。\
この仕組みは `e2e/pages/ResultPage.ts` にあります。\
ページ遷移そのものを検証するテストだけは、実際の UI 操作を維持しています。

待ち時間も同様です。\
PDF 生成のポーリングは実時間で待つため、既定値のままだと 1 テストに数十秒かかります。\
`usePdfStatus.ts` の待ち時間は `durationFromEnv()` を通して環境変数で上書きでき、\
`playwright.config.ts` の `webServer.env` から短い値を渡します。\
既定値は本番向けのまま、テスト実行時だけ短縮しています。

## AWS Lambda + Puppeteer による非同期 PDF 生成

### 非同期 Invoke とポーリングによる完了検知

Lambda は `InvocationType.EVENT` で呼び出しています（`LambdaPdfService`）。\
非同期 Invoke なので、呼び出し元が受け取れるのは受理を示す 202 だけで、生成の成否は分かりません。

Webhook による完了通知は、SNS や SQS の追加で構成が重くなるため見送りました。\
採用したのは、S3 にオブジェクトが存在することを完了とみなす方式です。

`PdfController` の `GET /api/pdf/{id}/status` が `headObject` を 1 回実行し、\
存在すれば有効期限 10 分の署名付き URL を、無ければ生成中を返します。\
ポーリングのループはフロントエンド側（`usePdfStatus.ts`）が回し、\
バックエンドは状態を持たないエンドポイントに留めています。

待ち時間の扱いは次の定数で制御しています。

| 定数                | 既定値 | 役割                                                      |
| ------------------- | ------ | --------------------------------------------------------- |
| `INITIAL_DELAY_MS`  | 6 秒   | 初回ポーリングまでの待機（生成完了の実測を下回らない値）  |
| `FAST_INTERVAL_MS`  | 2 秒   | 生成中のポーリング間隔                                    |
| `SLOW_INTERVAL_MS`  | 5 秒   | 想定時間を超えてからのポーリング間隔                      |
| `SLOW_THRESHOLD_MS` | 20 秒  | これを超えたら表示を「時間がかかっています」へ切り替える  |
| `TIMEOUT_MS`        | 60 秒  | 打ち切り（Lambda 側のタイムアウト 30 秒＋往復のマージン） |

進捗率は取得できないため、進捗バーは `SLOW_THRESHOLD_MS` を 100% とした経過時間ベースの目安です。

打ち切ったあとに応答が遅れて返ることがあるため、`usePdfStatus.ts` は世代番号（`runToken`）を持ちます。\
停止のたびに繰り上げ、応答時に世代が変わっていればその結果は捨てます。

### 再試行による再生成

S3 の存在確認だけでは、生成中と生成失敗を区別できません。\
オブジェクトが現れない状態は両者で同じに見えるため、`TIMEOUT_MS` まで待って失敗と判断します。

`POST /api/pdf/{id}/regenerate` を用意し、「再試行」から呼びます。\
`LambdaPdfService` は直近の Invoke から 15 秒以内のリクエストをスロットリングするため、\
連打しても Lambda は起動しません。

この経路は、PDF の寿命がシート本体より短い場合にも使います（「共有 URL の設計」を参照）。\
生成に失敗した場合と、PDF が削除されたあとの再訪で、同じ操作を使います。

### 描画完了を判定する属性

Lambda は共有ページをブラウザで開いて PDF 化するため、\
どこまで描画が進んだら PDF にしてよいかを判定する必要があります。

クラス名や DOM 構造を判定に使うと、フロントエンドのリファクタで PDF 生成が壊れます。\
壊れてもテストが通ってしまうため、判定に使う属性を 2 つに限定し、それ以外は見ないようにしました。

- `data-pdf-ready` … `ResultPage.vue` のシートルート。ページが `ready` のときだけ出力される
- `data-pdf-error` … 同ファイルのエラーパネル。期限切れや未存在で表示される
  Lambda 側は両方を待ち、エラー側が出ていれば PDF を作らずに失敗させます。

```ts
await page.waitForSelector('[data-pdf-ready], [data-pdf-error]', {
  timeout: 10000,
});

if ((await page.$('[data-pdf-error]')) !== null) {
  throw new Error(
    `シートを描画できませんでした（期限切れ／未存在の可能性）: id=${id}`,
  );
}
```

2 つの属性は排他です。\
同時に出ると不正な PDF が生成されるため、`e2e/share.spec.ts` に排他性の検証を置いています。\
属性名を変える場合は `aws-lambda/src/index.ts` も同時に変更が必要で、双方にコメントを残しています。

PDF はライトモード固定です。\
`emulateMediaFeatures` によるカラースキームの指定は `goto` の前に実行します。\
`index.html` の FOUC 対策スクリプトより後では間に合わないためです。

### URL と入力値の検証

この Lambda は渡された URL をブラウザで開くため、\
URL を差し替えられると、外部から到達できない内部リソースにアクセスされる恐れがあります。\
`aws-lambda/src/index.ts` の `assertAllowedUrl()` が URL のオリジンを許可値と照合し、\
一致しなければ PDF を生成せず失敗させます。

呼び出し元はバックエンドだけなので、この検証は現状では冗長です。\
呼び出し元の設定ミスや将来の変更に備えて、実行する側でも確認しています。

`id` も検証します。\
S3 のキーを `skill-sheets/${id}.pdf` で組み立てるため、\
UUID の形式に合わない値を渡されると、想定外のキーを指定される可能性があります。\
ファイル名は `sanitizeFileName()` を通します。`Content-Disposition` に利用者の入力が載るためです。

### Puppeteer のコンテナ化

Lambda 上で Puppeteer を安定動作させるには、依存ライブラリを含めた環境構築が必要です。\
日本語フォントは軽量ランタイム（`@sparticuz/chromium`）では fontconfig が反映されず崩れました。\
フォントを埋め込む対応も試しましたが、安定した日本語表示を優先し、\
フル版の Puppeteer（Chrome for Testing）を Docker でコンテナ化する方針に切り替えました。

## 共有 URL の設計

### 共有 ID と有効期限

共有相手にログインを求めない方針のため、URL を知っていることだけが閲覧の条件です。\
ID が推測できると他人のシートを閲覧できてしまうので、連番は使えません。\
ID は JPA の `GenerationType.UUID` で発行しています。

URL が漏れた場合に備えて、有効期限を設けています。\
期限切れは 410、削除後は 404 で区別し（`GlobalExceptionHandler`）、\
`@Scheduled` が期限切れのシートを毎日削除します。

### PDF とシートで寿命を分ける

PDF は S3 のライフサイクルルールで、最終更新から 1 日後に削除しています。\
シート本体の有効期限は 5 日なので、シートだけが残る期間が最大 4 日あります。\
PDF を長期間置かないための設定で、再訪時は `regenerate` で作り直します。

この削除は AWS 側の設定です。\
バックエンドが `@Scheduled` で消しているのは DB のレコードだけです。

### クローラからの除外

ルーターが `createWebHashHistory` のため、共有 URL は `/#/result?id=...` の形です。\
ハッシュより後ろはサーバーへ送られず、サーバーから見ればすべてのページが `/` になります。\
`robots.txt` の `Disallow` も Vercel の `X-Robots-Tag` もパス単位でしか指定できず、\
共有ページだけをクローラから外せません。

`createWebHistory` への移行は、Vercel の rewrite 設定、Lambda が組み立てる URL、\
e2e のロケータまで影響するため、クローラ対策のためだけには見合いません。\
検索流入を必要としないアプリなので、`index.html` の `<head>` でサイト全体を `noindex` にしています。

## Railway と Lambda による処理の分離

バックエンドの API は Railway、PDF 生成は AWS Lambda に分けています。処理を分けた理由は2つあります。

1つは、稼働環境のメモリに余裕がないことです。実務での利用機会を優先して Spring Boot を選びましたが、起動時のメモリ消費が大きく、全体の構成を詰めないと維持コストが膨らみます。\
PDF 生成は Chromium の起動を伴うため、API と同じコンテナに載せると生成中はメモリと CPU を占有し、無関係なリクエストの応答にも影響します。

もう1つは、負荷のかかり方が違うことです。API は常時稼働が前提で負荷は平坦ですが、PDF 生成は発生しない時間が長く、発生すると重い処理が集中します。\
Lambda はリクエスト単位で起動するため、生成が同時に重なっても API 側には影響せず、生成のない時間帯のコストも発生しません。

代償として、コールドスタートによる起動の待ちが発生します。これに対しては[プリフェッチ設計](#lambda-のコールドスタートを見越したプリフェッチ設計)で対応しています。

## CI/CD の構成

### 変更されたコンポーネントだけを走らせる

一つのリポジトリにフロントエンド・バックエンド・Lambda が同居するため、\
毎回すべてのテストを回すと待ち時間が伸びます。\
`dorny/paths-filter` で変更されたディレクトリを検出し、該当するジョブだけを実行します。

e2e は実行コストが大きいので、プルリクエストでは chromium だけ、\
`main` への push では 3 ブラウザを並列で回します。

### テストの成功をデプロイの前提にする

デプロイはテストとは別のワークフローに分け、`workflow_run` で連結しています。\
`needs` で繋がないのは必要な権限が違うためで、テスト側は `contents: read` だけで済みますが、\
デプロイ側はレジストリへの書き込みと OIDC のトークン発行を必要とします。

変更検出の結果は、テスト側でファイルに書き出してアーティファクトとして渡します。\
ワークフローをまたぐため、ジョブの `outputs` では受け渡せません。

### コンテナのビルドと配布

バックエンドは GHCR へ push して Railway を再デプロイし、\
Lambda は OIDC で AWS へ認証して ECR へ push し、`update-function-code` で適用します。\
ビルドをデプロイ先ではなく GitHub Actions 側で行い、デプロイ先の負荷を下げています。

Lambda 側では `docker/build-push-action` の `provenance` と `sbom` を無効化しています。\
buildx は既定で attestation を付与し、イメージが OCI Image Index（Manifest List）になりますが、\
Lambda は Manifest List を受け付けません。\
同じ制約は GHCR → Railway 側には無いため、そちらは OCI のままです。

## その他の設計判断（バックエンド／インフラ）

- **保存 API のレート制限** … シート作成は必ず Lambda の起動を伴うため、\
  同一クライアントから 10 分間に 20 件を上限とし、超過時は 429 と `Retry-After` を返す。\
  フロント側の制御では API を直接呼ばれた場合を防げないため、サーバー側で制限している
- **既存の共有画面を活用した PDF 生成** … PDF 専用の描画ロジックを別に作らず、\
  共有ページをそのまま Puppeteer で開く。実装コストと、画面と PDF の表示の一貫性を両立
