FROM eclipse-temurin:21-jdk-alpine
WORKDIR /app

COPY .mvn/ .mvn/
COPY mvnw pom.xml ./
RUN chmod +x mvnw
RUN ./mvnw dependency:go-offline -B

COPY src ./src

EXPOSE 8080

# JARを作成せず、ソースコードのまま起動してDevToolsによるホットリロードを有効にする
CMD ["./mvnw", "spring-boot:run"]